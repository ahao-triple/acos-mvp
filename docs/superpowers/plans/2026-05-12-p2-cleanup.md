# P2/5 + P2/6 收尾合并 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 CHANGES-BEFORE-AGENTS.md 最后两条 P2 一起落地：建 `difference-hunt/.env.example`、记录"刻意不引入 formatter"决策。

**Architecture:** 纯文档/配置改动；无代码、无依赖、无 CI 影响。一个 commit 完成两件事的核心改动 + 一个 commit 打勾。

**Tech Stack:** 无新工具/依赖。

**Spec:** `docs/superpowers/specs/2026-05-12-p2-cleanup-design.md`

---

## File Structure

- **Create**: `games/difference-hunt/.env.example`
- **Modify**: `CHANGES-BEFORE-AGENTS.md`（P2/5 段 + P2/6 段）
- **不动**：mini-pack、其它 games/、scripts/、package.json、pnpm-lock.yaml、CI workflow、`gonglian-fangxian/.env.example`

---

## Task 1: 建 `games/difference-hunt/.env.example` + 原子 commit

**Files:**
- Create: `games/difference-hunt/.env.example`

- [ ] **Step 1: 建文件**

Write `games/difference-hunt/.env.example` 内容（字面、不改）：

```
DOUYIN_APPID=
DOUYIN_REWARDED_AD_UNIT_ID=
KUAISHOU_APPID=
KUAISHOU_REWARDED_AD_UNIT_ID=

# 可选：覆盖默认 serverBaseUrl（构建时注入，改完需重新 pnpm build）
# 设为空行等价于不设置，使用默认值（用 || 而非 ??）
# 注意：发版前确认此项已注释或已还原，否则测试地址会被打进产物
# SERVER_BASE_URL=https://staging.example.com/api
```

> 注：vivo 不需要 env（`materials.ts` 全硬编码），故不列 VIVO_*。SERVER_BASE_URL 注释段与 `gonglian-fangxian/.env.example` 字面一致。

- [ ] **Step 2: 复核**

Run:
```bash
cat games/difference-hunt/.env.example
diff games/gonglian-fangxian/.env.example games/difference-hunt/.env.example
```

Expected:
- 第一条 cat 输出上面写入的 9 行内容
- diff 显示前 4 行 env key 不同（gonglian 只有 DOUYIN_*，difference-hunt 多 KUAISHOU_*），SERVER_BASE_URL 注释段字面一致

- [ ] **Step 3: `git status` 确认只有这一个新文件**

Run:
```bash
git status
```

Expected: 未跟踪 `games/difference-hunt/.env.example`，无其它改动。

- [ ] **Step 4: Stage + commit**

Run:
```bash
git add games/difference-hunt/.env.example
git commit -m "$(cat <<'EOF'
docs(env): 建 difference-hunt/.env.example

与 gonglian-fangxian/.env.example 风格对齐：列 DOUYIN/KUAISHOU 两套
env key + SERVER_BASE_URL 注释段（含空串语义与发版警告）。
vivo 不需要 env（materials.ts 全硬编码 packageName/versionName）
所以未列 VIVO_*。

对应 CHANGES-BEFORE-AGENTS.md 的 P2/5。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: CHANGES-BEFORE-AGENTS.md P2/5 + P2/6 打勾（独立 commit）

**Files:**
- Modify: `CHANGES-BEFORE-AGENTS.md`（两段）

- [ ] **Step 1: 改 P2/5 段**

`CHANGES-BEFORE-AGENTS.md` 当前 P2/5 段开头：

```markdown
## [P2] 5. 给 `difference-hunt` 也加 `.env.example`

- [ ] 完成
```

把 `- [ ] 完成` 改为：

```markdown
- [x] 完成

建了 `games/difference-hunt/.env.example`，含 DOUYIN/KUAISHOU 两套 env
key + SERVER_BASE_URL 注释段，与 `gonglian-fangxian/.env.example` 对齐。
vivo 不需要 env（materials.ts 全硬编码）所以未列。
不补 preflight env 校验——build 内部 materials 校验已给清晰错误，
preflight 重复造防御无价值。
见 `docs/superpowers/specs/2026-05-12-p2-cleanup-design.md`。
```

- [ ] **Step 2: 改 P2/6 段**

`CHANGES-BEFORE-AGENTS.md` 当前 P2/6 段开头：

```markdown
## [P2] 6. 选一个最轻量的 formatter（可选）

- [ ] 完成（或主动决定不做）
```

把 `- [ ] 完成（或主动决定不做）` 改为：

```markdown
- [x] 完成 — 决定**刻意不引入**

理由：MVP 阶段团队/代码量都小，引入 Biome/Prettier 的成本（config +
全仓库 format + 修 diff 噪音 + CI lint step）超过收益。等团队/代码量
上来再选 Biome（单二进制、零配置、与 mini-pack 的 esbuild 风格匹配）。

写 AGENTS.md 时请抄入一句：
> Repository intentionally has no linter/formatter; do not add one without discussion.

见 `docs/superpowers/specs/2026-05-12-p2-cleanup-design.md`。
```

- [ ] **Step 3: 复核 + commit**

Run:
```bash
git diff CHANGES-BEFORE-AGENTS.md
```

Expected: diff 只命中 P2/5 段与 P2/6 段两个 `- [ ]` 行 + 各自下方加的注脚段；P0/1、P0/2、P1/3、P1/4 段保持原样。

然后 stage + commit：
```bash
git add CHANGES-BEFORE-AGENTS.md
git commit -m "$(cat <<'EOF'
docs: 标记 CHANGES-BEFORE-AGENTS.md P2/5 + P2/6 完成

P2/5：difference-hunt/.env.example 已建（见上一 commit）。
P2/6：决定刻意不引入 formatter，理由与退出条件记在注脚里；
写 AGENTS.md 时抄入"intentionally no linter/formatter"那句。

清单已全部清完（P0/1、P0/2、P1/3、P1/4、P2/5、P2/6），下一步
是写 AGENTS.md。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: Verify 全套

**Files:** 无文件改动；只跑命令观察

- [ ] **Step 1: 新文件存在且内容正确**

Run:
```bash
ls games/difference-hunt/.env.example
wc -l games/difference-hunt/.env.example
cat games/difference-hunt/.env.example
```

Expected:
- ls 命中
- wc -l 输出 `9 ...`（4 env key + 1 空行 + 3 注释行 + 1 注释 SERVER_BASE_URL = 9 行；如有尾换行可能 10）
- cat 输出与 Task 1 Step 1 写入内容一致

- [ ] **Step 2: 与 gonglian-fangxian 的注释段对齐**

Run:
```bash
diff <(tail -4 games/gonglian-fangxian/.env.example) <(tail -4 games/difference-hunt/.env.example)
```

Expected: 无输出（两份 `.env.example` 末尾 4 行——3 行注释 + 1 行注释掉的 SERVER_BASE_URL ——逐字一致）。

如果 diff 有输出，回 Task 1 Step 1 复核注释段。

- [ ] **Step 3: test:pack 不受影响**

Run:
```bash
pnpm test:pack 2>&1 | tail -10
```

Expected: 输出含 `Tests  72 passed` 或类似（1 skipped 也接受），退出码 0。

> 注：本步骤验证新增 `.env.example` 不影响 `repo-build.test.ts`。`.env.example` 是参考文档，mini-pack 加载的是 `.env`，不读 `.env.example`，预期完全无影响。

- [ ] **Step 4: lockfile 未动**

Run:
```bash
git diff origin/main -- pnpm-lock.yaml
```

Expected: 无输出（lockfile 未动）。

- [ ] **Step 5: 改动文件总数符合预期**

Run:
```bash
git diff --stat origin/main
```

Expected: 显示 4 个文件改动——
- `CHANGES-BEFORE-AGENTS.md` 修改
- `docs/superpowers/plans/2026-05-12-p2-cleanup.md` 新增
- `docs/superpowers/specs/2026-05-12-p2-cleanup-design.md` 新增
- `games/difference-hunt/.env.example` 新增

> 注：spec 与 plan 是 controller 在 worktree 内提前 commit 的（不在本 plan 的两个 task 范围里）。如果显示更多文件，停下排查。

- [ ] **Step 6: `git log --oneline -5`**

Expected: 最新两条是 Task 2 + Task 1 的 commit；再往下是 spec/plan + main 头。

---

## Task 4: 合并 + push（controller 执行）

> 注：本 task 不在 implementer 范围，由 controller 完成。

- [ ] **Step 1: fast-forward merge**

```bash
cd /Users/apple/Documents/codex_projects/acos-mvp
git merge --ff-only p2-cleanup
git log --oneline -6
```

- [ ] **Step 2: 清理 worktree 与分支**

```bash
git worktree remove ../acos-mvp.p2
git branch -d p2-cleanup
```

- [ ] **Step 3: push**

```bash
git push origin main
```

Expected: push 成功；CI 自动触发（typecheck/test:game/test:pack），全绿。

---

## Self-Review 结果

- **Spec 覆盖**：
  - "建 difference-hunt/.env.example 与 gonglian 对齐" → Task 1
  - "CHANGES P2/5、P2/6 打勾" → Task 2
  - "刻意不引入 formatter，理由 + 退出条件 + AGENTS.md 一句话" → Task 2 Step 2 注脚
  - "不动 AGENTS.md/biome.json/preflight" → 改动清单不含；Verify Step 5 用 `--stat` 兜底
  - Verify 6 条 → Task 3 Step 1-6
- **Placeholder 扫描**：无 TBD/TODO；每条 commit message 完整 HEREDOC
- **类型/字段一致性**：`games/difference-hunt/.env.example` 字面内容在 spec、plan Task 1 Step 1 一致；CHANGES 注脚措辞在 spec 与 plan 一致
