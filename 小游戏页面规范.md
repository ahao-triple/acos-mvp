# 小游戏页面规范文档

> 文档版本：v1.2
> 适用范围：休闲类小游戏（微信 / 抖音 / QQ / H5 等）
> **使用原则**：本文档仅规定游戏需要具备的**功能与结构**，不限制功能的具体**表现形式**（视觉、动效、文案风格、动画、配色等由具体游戏自行设计）。

---

## 一、页面整体结构

游戏共包含 **4 个核心页面 + 3 个通用弹窗**：

| 编号 | 页面名称 | 页面 ID | 作用 |
|------|---------|---------|------|
| P1 | 启动加载页 | `LoadingPage` | 资源加载、品牌曝光、合规信息展示 |
| P2 | 主页 | `HomePage` | 游戏功能入口、关卡选择、用户信息 |
| P3 | 游戏页 | `GamePage` | 实际游戏玩法 |
| P4 | 结算页 | `ResultPage` | 展示结果、下一步引导 |

### 1.1 页面跳转流程

```
LoadingPage
    │ (进度条 1-2 秒后自动跳转)
    ▼
HomePage  ◄──────────────┐
    │ (点击开始游戏)       │
    ▼                     │
GamePage                  │
    │ (游戏结束)           │
    ▼                     │
ResultPage ───────────────┘
   （下一关 / 重玩 / 返回主页）
```

---

## 二、通用规范

### 2.1 全局通用元素
- **顶部状态栏占位**：所有页面需预留刘海屏 / 灵动岛安全区
- **左上角金币**：当游戏有金币机制时，统一在左上角展示

### 2.2 文字规范
- 所有用户可见文字必须为**简体中文**
- **禁止使用**：英文、繁体字、生僻字
- **唯一例外**：标准化合规字符（如软著号 `SR` 前缀、CADPA 适龄图标内置字符等监管要求字符）

### 2.3 通用交互
- **Toast（轻提示）**：**调用平台原生 Toast 能力**，不自定义实现
- **Dialog（弹窗）**：
  - 必须带**黑色半透明背板**
  - 所有弹窗必须含**关闭按钮**
  - 点击背板关闭与否由各弹窗自行决定，但关闭按钮必须存在

### 2.4 广告规范
- 本游戏仅接入**激励视频广告**，不使用 banner、插屏等其他广告形式
- 所有"看广告"按钮必须带**广告标识图标**（图标资源见 [§六 图片资源清单](#六图片资源清单)）
  - 广告标识与按钮主文案需做好排版，**禁止重叠、禁止挤出按钮范围**
- 所有看广告流程结束后**统一弹 toast** 提示（赞助、入口奖励、关卡解锁、复活、跳过本关、游戏内道具等所有入口均适用）

---

## 三、页面详细规范

### P1 启动加载页（LoadingPage）

**作用**：游戏冷启动展示页，承载品牌曝光与合规信息。

**进入时机**：游戏冷启动

**离开时机**：进度条动画结束（1-2 秒）后自动跳转 HomePage

#### 元素清单

页面所有元素均为**纯展示**，无任何交互。

| # | 元素 | 类型 | 内容 / 说明 |
|---|------|------|------------|
| 1 | 著作权人 | 文字 | 顶部，简体中文 |
| 2 | 软著登记号 | 文字 | 顶部，格式 `SRxxxxxxxxxx` |
| 3 | 游戏主标题 | 文字 | 居中，简体中文 |
| 4 | 副标题（可选） | 文字 | 主标题下方，**如有则必须为简体中文** |
| 5 | 状态文案 | 文字 | 简体中文短句 |
| 6 | 装饰动画 | 图片 / 动画 | 氛围装饰 |
| 7 | 进度条 | 装饰元素 | 动画时长 1-2 秒，**不绑定真实加载进度** |
| 8 | 提示文案 | 文字 | 简体中文短句 |
| 9 | 适龄提示图标 | PNG / 矢量 | 左下角，资源与实现见 [§六](#六图片资源清单) |
| 10 | 健康游戏忠告 | 文字 | 底部，含标题 + 4 行 8 句固定文本 |

#### 交互逻辑
- 页面无任何用户交互
- 进度条动画时长 1-2 秒，结束后自动跳转 HomePage
- 装饰动画与进度条为纯视觉效果

#### 文案规范

**健康游戏忠告（含标题，固定文本，不可修改）**：

> **健康游戏忠告**
>
> 抵制不良游戏，拒绝盗版游戏。
> 注意自我保护，谨防受骗上当。
> 适度游戏益脑，沉迷游戏伤身。
> 合理安排时间，享受健康生活。

---

### P2 主页（HomePage）

**作用**：游戏主入口，承载功能按钮、关卡选择与游戏开始。

**进入时机**：LoadingPage 自动跳转 / 从 ResultPage 返回

#### 元素清单

| 区域 | 元素 | 类型 | 触发结果 |
|------|------|------|---------|
| 左上角 | 金币显示 | 文字 + 图标 | 仅当游戏有金币机制时显示 |
| 按钮区 | 用户信息按钮 | 按钮 | 打开「设置弹窗」 |
| 按钮区 | 加桌按钮 | 按钮 | 触发平台加桌能力 |
| 按钮区 | 设置常用小游戏按钮 | 按钮 | 触发平台"设为常用"能力 |
| 按钮区 | 入口奖励按钮 | 按钮 | 打开「入口奖励弹窗」（**仅抖音平台**显示） |
| 按钮区 | 关卡选择按钮 | 按钮 | 打开「关卡选择弹窗」 |
| 主 CTA | 开始游戏按钮 | 大按钮 | 进入 GamePage（当前关卡） |

#### 默认范围说明
- **不包含**：头像 / 昵称、签到、排行榜、商城、任务、成就等
- 如某具体游戏需要上述功能，由该游戏自行扩展

---

### P3 游戏页（GamePage）

**作用**：承载实际游戏玩法。

**进入时机**：从 HomePage 点击「开始游戏」/「关卡」/「重玩」等

#### 元素清单

| 区域 | 元素 | 说明 |
|------|------|------|
| 左上角 | 金币显示 | 仅当游戏有金币机制时显示 |
| 右上角 | 设置按钮 | 打开「设置弹窗」（与 HomePage 用户信息按钮共用同一弹窗） |
| 顶部其他 | 关卡数 / 计时 / 分数 / 目标提示等 | **由具体游戏自行设计**，文档不强制 |
| 主区域 | 游戏画布 | 承载核心玩法 |
| 底部 | 道具按钮 ×2-3 | **必需** |

**道具按钮规则**：
- 库存 >0 时点击直接消耗使用
- 库存 = 0 时点击触发激励视频广告，看完弹 toast 并获得使用机会
- 按钮名称（如"撤销""提示""刷新"等）由具体游戏自定义
- 按钮上需带广告标识（库存=0 状态），排版规则见 §2.4

#### 交互逻辑
- 游戏结束（成功 / 失败）→ 跳转 ResultPage
- 广告加载失败需 toast 提示且不消耗道具

---

### P4 结算页（ResultPage）

**作用**：展示本局游戏结果，引导用户进入下一步操作。

**进入时机**：游戏结束（成功或失败）

#### 4.1 结果信息区

**由具体游戏自行设计**。文档仅要求让用户明确知晓"成功 / 失败"结果，其余如标题文案、核心指标（步数 / 用时 / 分数 / 星级）、奖励展示、数据对比等内容由各游戏自定义。

#### 4.2 操作按钮区

| # | 元素 | 说明 |
|---|------|------|
| 1 | 下一关 | **仅通关时显示**，主 CTA |
| 2 | 重玩本关 | 失败 / 成功都显示 |
| 3 | 复活 / 继续 | **仅失败时显示**，看广告 → 复活 → toast（"原地复活"或"重玩本关"由具体游戏自定义） |
| 4 | 分享按钮 | 分享给好友 / 朋友圈，分享奖励内容由游戏自定义 |
| 5 | 返回主页 | 次要按钮，返回 HomePage |

带广告的按钮（如复活）必须按 §2.4 规则添加广告标识。

#### 成功 vs 失败 状态差异

| 元素 | 通关成功 | 挑战失败 |
|------|---------|---------|
| 主 CTA | 下一关 | 复活（看广告）/ 重玩 |
| 复活按钮 | 不显示 | 显示 |
| 下一关按钮 | 显示 | 不显示 |

#### 交互逻辑
- 「复活」按钮触发激励视频，看完后弹 toast，继续游戏
- 「分享」分享成功后发放奖励，需做防刷机制
- 「下一关」直接进入下一关 GamePage，不返回主页

---

## 四、通用弹窗规范

> 所有弹窗均需**黑色半透明背板** + **关闭按钮**（见 §2.3）。

### 4.1 设置弹窗（统一弹窗，Home 与 Game 共用）

HomePage 入口按钮叫「用户信息」，GamePage 入口按钮叫「设置」，弹窗内容共用同一套，仅显示项不同。

| # | 内容 | Home | Game | 说明 |
|---|------|:---:|:---:|------|
| 1 | 用户编号 | ✅ | ✅ | 7 位数字 |
| 2 | 赞助按钮（带广告标识） | ✅ | ✅ | 点击播放激励视频 → 看完弹 toast |
| 3 | 音乐开关 | ✅ | ✅ | 默认开 |
| 4 | 音效开关 | ✅ | ✅ | 默认开 |
| 5 | 返回桌面 | ✅ | ✅ | 平台能力，退出小游戏到平台桌面 |
| 6 | 重新开始 | ❌ | ✅ | **仅游戏页显示**，重置本关回到起点 |
| 7 | 跳过本关（带广告标识） | ❌ | ✅ | **仅游戏页显示**，看广告 → 当作通关 → 进入 ResultPage 成功态 + toast |

### 4.2 入口奖励弹窗（仅抖音平台）

- **触发**：用户点击 HomePage 入口奖励按钮
- **行为**：弹窗呈现奖励内容，完成后弹 toast
- **非抖音平台**：HomePage 不显示入口奖励按钮，本弹窗不存在

### 4.3 关卡选择弹窗

| 状态 | 交互 |
|------|------|
| 已通关 | 点击可重玩 |
| 当前关 | 点击直接开始 |
| 未解锁 | 点击 → 看广告 → 解锁 → toast |

**解锁规则**：默认按通关顺序解锁下一关；额外提供看广告解锁能力（允许跳关 / 提前解锁指定关卡）。

未解锁关卡的看广告按钮（或点击触发的二次确认弹窗按钮）需带广告标识。

---

## 五、合规要素清单

启动加载页必须展示：
- ✅ 适龄提示图标（左下角，CADPA 12+，矢量绘制；PNG 备用素材见 §六）
- ✅ 软著登记号
- ✅ 著作权人
- ✅ 健康游戏忠告（含标题 + 4 行 8 句固定文本）

---

## 六、图片资源清单

以下图标资源由开发自行填入路径，本文档仅明确"必须有哪些资源 + 使用场景"：

| 图标 | 使用场景 | 实现方式 | 资源 / 代码位置 |
|------|---------|---------|---------|
| 适龄提示图标（CADPA 12+） | LoadingPage 左下角 | Canvas 矢量绘制（当前生效） | `src/render/loadingScreen.ts` → `drawAgeRatingVector` |
| 适龄提示图标（CADPA 12+） | LoadingPage 左下角 | PNG（备用，注释中） | `public-pack/assets/age-rating-12plus.png`（558×712） |
| 广告标识图标 | 所有看广告按钮通用 | Canvas 矢量绘制 | `src/render/uiPrimitives.ts` → `drawAdVideoIcon` |

> **关于矢量 vs PNG**：当前 LoadingPage 左下角的 12+ 适龄标识用 Canvas 矢量绘制（外框白底黑边 + 蓝色块 + 白字 "12+ / CADPA" + 黑字 "适龄提示"，整体 100×128 px，满足 CADPA ≥100×100 合规下限）。官方 PNG 已存放在 `public-pack/assets/age-rating-12plus.png`，相关加载代码在 `loadingScreen.ts` 中以注释保留，如矢量版被平台审核驳回，取消两处注释即可切换为 PNG 渲染。
>
> **资源目录注意事项**：本项目 `vite.config.ts` 将 `publicDir` 配置为 `public-pack`（不是默认的 `public`），所有静态资源必须放在 `public-pack/` 下才能通过 `/` 根路径访问。

> 其他图标（Logo、按钮图标、关卡状态、星级、分享、关闭等）由具体游戏自行设计，本文档不做限制。

---

## 七、本规范不包含的内容

以下功能 / 内容**默认不在本规范范围内**，若某具体游戏需要，由该游戏自行扩展实现：

- 用户协议 / 隐私政策 / 关于 / 版本号
- 实名认证 / 防沉迷系统
- 签到 / 排行榜 / 商城 / 任务 / 成就
- 网络异常处理
- 首启隐私政策弹窗
- 顶部 HUD 详细信息（关卡数、计时、目标提示等）
- 结算页结果信息区（标题、指标、奖励展示等具体内容）
- 数据埋点
- 新手引导流程
- 视觉规范、动效规范、音效规范

---

*文档结束 — 建议配合 UI 视觉稿与交互原型一同评审*
